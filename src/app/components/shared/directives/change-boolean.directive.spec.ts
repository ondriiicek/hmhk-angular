import { ChangeBooleanDirective } from './change-boolean.directive';

describe('ChangeBooleanDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBooleanDirective();
    expect(directive).toBeTruthy();
  });
});
